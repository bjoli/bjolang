;; A library module with state of its own, to check that a module-level
;; `def/mutable` survives the module boundary rather than being copied.
(export tick reads reset)

(: reads int)
(def/mutable reads 0)

(: tick (-> int))
(defun (tick)
  (set! reads (+ reads 1))
  reads)

(: reset (-> void))
(defun (reset)
  (set! reads 0))
