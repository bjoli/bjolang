(defun (main args)
    (def result
        (let loop ((count 5) (acc 1))
            (if (= count 0)
                acc
                (loop (- count 1) (* acc count)))))
    
    (displayln (int->string result))
    0)
