;; Nested include: this file itself includes another one, and carries an
;; import that must become a dependency of the *including* module.
(import (std core))

(include "math_helpers.bjo")

(: describe (-> int int))
(defun (describe n)
  (println (square n))
  (println (cube n))
  n)
