;; Included into 17_include.bjo. Not a module of its own: these definitions
;; become part of whatever file includes this one, so they need no export.

(: square (-> int int))
(defun (square x)
  (* x x))

(: cube (-> int int))
(defun (cube x)
  (* x (square x)))
