;; The third module. Neither the program nor the module that defines the impl
;; is where this lives — the inlined body has to reach all the way back here.
(export decorate)

(: decorate (-> string string))
(defun (decorate s) (string-append "<" (string-append s ">")))
