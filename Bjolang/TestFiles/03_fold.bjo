(import (std list))

(defun (foldl f init lst)
  (if (empty? lst)
      init
      (foldl f (f init (head lst)) (tail lst))))

(defun (main argv)
  (def lst (Cons 1 (Cons 2 (Cons 3 Nil))))
  (displayln (int->string (foldl (fun (acc x) (+ acc x)) 0 lst)))
  0)
