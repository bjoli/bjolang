(: f (-> int (#:acc int) (#:other int) int))
(defun (f n #:acc (let loop ((i 3) (a 0)) (if (= i 0) a (loop (- i 1) (+ a 1)))) #:other (+ acc 100))
  (+ (+ n acc) other))

(defun (main)
  (displayln (int->string (f 1))))
