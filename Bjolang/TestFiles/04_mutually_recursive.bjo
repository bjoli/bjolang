;; Entry point — defs and expressions can be interleaved
(defun (main)
  (defun (is-even? n)
      (or (= n 0) (is-odd? (- n 1))))
  (defun (is-odd? n)
    (and (not (= n 0)) (is-even? (- n 1))))
  (def res (is-odd? 411))
  (displayln (if res "Right" "Wrong")))
