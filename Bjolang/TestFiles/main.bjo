(import (std list))
(import "dummy_lib")

(: main (-> (List string) int))
(defun (main args)
  (def lst (Cons "a" (Cons "b" Nil)))
  (defun (f x) x)
  (def mapped (list-map lst f))
  (displayln (list-head mapped))
  0)
