;; std/prelude.bjo
;; Standard library entry point

(import
    (std core)
    (std list)
    (std iter))

;; The aggregate surface of the standard library. Names listed here are visible
;; to anything that imports (std prelude); nothing else is.
(re-export print println)

;; Placeholder so the aggregate module has a body.
(: noop (-> int))
(defun (noop) 0)
